{
  "name": "pane-e-amore-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd Frontend && npm install && cd ../Backend && npm install",
    "frontend": "cd Frontend && npm run dev",
    "backend": "cd Backend && npm run dev",
    "cleanup": "node cleanup.js",
    "devstart": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
